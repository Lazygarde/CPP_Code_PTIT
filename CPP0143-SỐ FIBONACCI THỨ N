#include <bits/stdc++.h>
#define endl "\n"
using namespace std;
long long fibo(int n){
	if(n==1||n==2) return 1;
	return fibo(n-1)+fibo(n-2);
}
int main(){
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	int t;
	cin>>t;
	while(t--){
		int n;
		cin>>n;
		cout<<fibo(n)<<endl;
	}
}
