#include <bits/stdc++.h>
#define endl "\n"
using namespace std;
int main(){
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	int t;
	cin>>t;
	while(t--){
		int n;
		cin>>n;
		int a[n],b[n]={};
		for(int i=0;i<n;i++){
			cin>>a[i];
			if(a[i]>=0&&a[i]<n) b[a[i]]++;
		}
		for(int i=0;i<n;i++){
			if(b[i]>0) cout<<i<<" ";
			else cout<<-1<<" ";
		}
		cout<<endl;
	}
}
