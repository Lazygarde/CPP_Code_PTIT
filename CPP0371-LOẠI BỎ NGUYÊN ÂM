#include <bits/stdc++.h>
using namespace std;
int main(){
	string a;
	cin>>a;
	int n=a.size();
	for(int i=0;i<n;i++){
		if(a[i]>='A'&&a[i]<='Z') a[i]+=32;
		if(a[i]=='e'||a[i]=='u'||a[i]=='o'||a[i]=='a'||a[i]=='i') continue;
		cout<<'.'<<a[i];
	}
}
