#include <bits/stdc++.h>
#define endl "\n"
using namespace std;
int main(){
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	int t;
	cin>>t;
	while(t--){
		int n;
		cin>>n;
		int a[n],s=0;
		for(int i=0;i<n;i++){
			cin>>a[i];
			if(a[i]==0) s++;
			else cout<<a[i]<<" ";
		}
		for(int i=0;i<s;i++) cout<<0<<" ";
		cout<<endl;
	}
}
