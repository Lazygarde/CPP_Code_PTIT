#include <bits/stdc++.h>
#define endl "\n"
using namespace std;
int main(){
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	int t;
	cin>>t;
	while(t--){
		int m,n,a,b,s=0;
		cin>>m>>n>>a>>b;
		for(int i=m;i<=n;i++){
			if(i%a==0||i%b==0) s++;
		}
		cout<<s<<endl;
	}
}
